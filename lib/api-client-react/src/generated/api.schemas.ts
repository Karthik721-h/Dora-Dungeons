/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Dora Dungeons API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface StartGameRequest {
  /** Optional player name */
  playerName?: string;
}

export interface ActionRequest {
  /** The voice command or action string (e.g. "attack goblin", "move north") */
  command: string;
}

export interface ParsedCommand {
  /** The parsed action type (e.g. ATTACK, MOVE, DEFEND, CAST_SPELL, LOOK) */
  action: string;
  /** The parsed target if any */
  target?: string;
  /** Direction for movement commands */
  direction?: string;
  /** The original raw command string */
  raw: string;
}

export interface WeaponState {
  id: string;
  name: string;
  description: string;
  price: number;
}

export interface ArmorState {
  id: string;
  name: string;
  level: 1 | 2 | 3;
}

export interface PlayerState {
  id: string;
  name: string;
  hp: number;
  maxHp: number;
  mp: number;
  maxMp: number;
  level: number;
  xp: number;
  xpToNextLevel: number;
  attack: number;
  defense: number;
  abilities: string[];
  inventory: string[];
  /** Weapons purchased from the shop */
  weapons: WeaponState[];
  /** Armors owned by the player */
  armors: ArmorState[];
  /** Full inventory details (id + value) for the shop sell view */
  inventoryItems: { id: string; name: string; value: number }[];
  /** Which dungeon depth the player is currently on (1-indexed) */
  dungeonLevel: number;
  /** True when the boss of the current dungeon has been defeated */
  dungeonLevelCompleted: boolean;
}

export interface EnemyState {
  id: string;
  name: string;
  hp: number;
  maxHp: number;
  attack: number;
  defense: number;
  xpReward: number;
  isDefeated: boolean;
}

export type RoomStateExits = { [key: string]: string };

export interface RoomState {
  id: string;
  name: string;
  description: string;
  exits: RoomStateExits;
  enemies: EnemyState[];
  items: string[];
  isExplored: boolean;
}

export interface GameStateResponse {
  sessionId: string;
  /** IDLE, IN_COMBAT, EXPLORING, GAME_OVER, VICTORY */
  gameStatus: string;
  /** Current gold amount (combat earned + shop balance) */
  gold: number;
  player: PlayerState;
  currentRoom: RoomState;
  /** Narrative messages describing what happened */
  logs: string[];
  /** New logs appended since last action (only present on action responses) */
  newLogs?: string[];
  /** The command that was parsed (only present on action responses) */
  parsedCommand?: ParsedCommand;
  turnCount: number;
  /** One-shot event emitted this turn (e.g. LEVEL_COMPLETED) */
  event?: string;
}

export interface ErrorResponse {
  error: string;
  message: string;
}

export interface AuthUser {
  id: string;
  email?: string | null;
  firstName?: string | null;
  lastName?: string | null;
  profileImageUrl?: string | null;
}

export interface AuthUserEnvelope {
  user: AuthUser | null;
}

export interface MobileTokenExchangeRequest {
  /** @minLength 1 */
  code: string;
  /** @minLength 1 */
  code_verifier: string;
  /** @minLength 1 */
  redirect_uri: string;
  /** @minLength 1 */
  state: string;
  /** @minLength 1 */
  nonce?: string;
}

export interface MobileTokenExchangeSuccess {
  token: string;
}

export interface LogoutSuccess {
  success: boolean;
}

/**
 * Bearer session token (used by mobile clients).
 */
export type AuthorizationSessionHeaderParameter = string;

export type BeginBrowserLoginParams = {
  /**
   * Relative path to redirect to after login (must start with `/`). Defaults to `/`.
   */
  returnTo?: string;
};

export type HandleBrowserLoginCallbackParams = {
  code?: string;
  state?: string;
  iss?: string;
};
